diff --git a/CMakeLists.txt b/CMakeLists.txt
index 931d9cf..9259628 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1416,9 +1416,11 @@ if(NOT LIBRETRO)
 				${IOSURFACE_LIBRARY}
 				${MULTITOUCH_SUPPORT_LIBRARY})
 
+			if(USE_VULKAN)  # dream-recomp local patch: no MoltenVK copy when Vulkan is disabled
 			add_custom_command(TARGET ${PROJECT_NAME} POST_BUILD
 				COMMAND ${CMAKE_COMMAND} -E copy "$ENV{VULKAN_SDK}/lib/libMoltenVK.dylib"
 				$<TARGET_FILE_DIR:flycast>/../Frameworks/libvulkan.dylib)
+			endif()
 		endif()
 	elseif(UNIX)
 		if(NOT BUILD_TESTING)
@@ -1551,3 +1553,8 @@ if(${CMAKE_GENERATOR} MATCHES "^Xcode.*|^Visual Studio.*")
 	file(GLOB_RECURSE SRC_FILES *.h *.cpp *.c *.cc *.mm)
 	source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${SRC_FILES})
 endif()
+
+# dream-recomp local patch: headless oracle entry point in the libretro core.
+if(LIBRETRO)
+	target_sources(${PROJECT_NAME} PRIVATE shell/libretro/dream_oracle.cpp)
+endif()
diff --git a/shell/libretro/libretro.osx.def b/shell/libretro/libretro.osx.def
index 53a556a..2458e7d 100644
--- a/shell/libretro/libretro.osx.def
+++ b/shell/libretro/libretro.osx.def
@@ -25,3 +25,4 @@ _retro_unload_game
 _retro_get_region
 _retro_get_memory_data
 _retro_get_memory_size
+_dream_oracle_run

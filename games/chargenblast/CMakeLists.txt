# CHARGEN BLAST. Everything is in cmake/DreamAddGame.cmake; this file exists so that adding a game is
# adding a directory. Add BOOT_TEST once the title boots far enough to be worth asserting on.
dream_add_game(chargenblast TITLE "CHARGEN BLAST")

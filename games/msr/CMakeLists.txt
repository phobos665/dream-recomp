# A third title, and the only one whose disc carries several code files: 1ST_READ.BIN is a loader
# and the game proper lives in MSR.BIN and 2_DP.BIN. Only the loader is configured, so almost
# everything falls back to the interpreter — which is exactly what a newcomer's first title looks
# like, and why it is worth having in the tree.
dream_add_game(msr TITLE "Metropolis Street Racer")
